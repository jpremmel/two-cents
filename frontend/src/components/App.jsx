import React from 'react';

const App = () => {

  return(
    <div>
      <Switch>
        {/* <Route exact path='/' render={() => < COMPONENT />} */}
      </Switch>
    </div>
  );
};

export default App;